---
--- Generated by EBGAME
--- Created by wbn.
--- DateTime: 2022/10/11 11:18:31
---

---@class OrderedTable : UnityEngine.MonoBehaviour OrderedTable
---======================== 面板变量 ========================
---=========================================================
local OrderedTable = class("OrderedTable", target)

--region ===================== 扩展的数据类型 =====================
--- 放自定义的结构体、枚举等

--endregion

--region ======================== 基类成员 ========================

--endregion

--region ======================== 基类方法 ========================

--endregion

--region ======================== Mono接口 ========================

-----@private
--function OrderedTable:Awake()
--
--end

---@private
function OrderedTable:Start()
    local nT = newT()
    nT["One"] = 1
    nT["Two"] = 2
    nT["Three"] = 3
    nT["Four"] = 4
    nT["Five"] = 5

    Debug.Log(F"count:{nT:count()}")
    for i, v in nT:opairs() do
        Debug.Log(F"key: {i} value: {v}")
    end

    Debug.Log("delete two key")
    nT:del("Two")--必须使用del()删除，不能等于nil删除
    for i, v in nT:opairs() do
        Debug.Log(F"key: {i} value: {v}")
    end

    Debug.Log("origin pairs")
    for i, v in nT:pairs() do
        Debug.Log(F"key: {i} value: {v}")
    end


    Debug.Log("ipairs test")
    nT = newT()
    nT[1] = 1
    nT[2] = 2
    nT[3] = 3
    nT[4] = 4
    nT[5] = 5
    nT[10] = 10

    for i, v in nT:ipairs() do
        Debug.Log(F"index: {i} value: {v}")
    end

    Debug.Log("origin pairs")
    for i, v in nT:pairs() do
        Debug.Log(F"index: {i} value: {v}")
    end
end

-----@private
--function OrderedTable:OnDestroy()
--    
--end

-----@private
--function OrderedTable:OnDisable()
--    
--end

-----@private
--function OrderedTable:OnEnable()
--    
--end

-----@private
--function OrderedTable:Update()
--    
--end

--endregion

return OrderedTable