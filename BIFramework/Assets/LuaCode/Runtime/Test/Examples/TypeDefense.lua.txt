---
--- Generated by EBGAME
--- Created by wbn.
--- DateTime: 2022/07/28 09:55:30
---

---@class TypeDefense : UnityEngine.MonoBehaviour TypeDefense
---======================== 面板变量 ========================
---=========================================================
local TypeDefense = class("TypeDefense", target)

--region ===================== 扩展的数据类型 =====================
--- 放自定义的结构体、枚举等

--endregion

--region ======================== 基类成员 ========================
--所有基本类型定义
TypeDefense._numberVar = 1
TypeDefense._stringVar = "hello"
TypeDefense._boolVar = false
TypeDefense._callbackVar = callback--初始化function类型
TypeDefense._csharpVar = weak--初始化C#类型
TypeDefense._unmanagedVar = unmanaged--初始化托管类型(可以为nil)
--endregion

--region ======================== 基类方法 ========================

--endregion

--region ======================== Mono接口 ========================

-----@private
--function TypeDefense:Awake()
--
--end

---@private
function TypeDefense:Start()
    --访问未定义变量
    try{
        function()
            self.unknownVar = 1
        end,
        catch = function(e)
            print(e)
        end
    }

    --类型赋值错误
    try{
        function()
            self._callbackVar = "some string"
        end,
        catch = function(e)
            print(e)
        end
    }

    try{
        function()
            self._boolVar = 1
        end,
        catch = function(e)
            print(e)
        end
    }
    
    self._callbackVar = function()  end
    self._csharpVar = CS.UnityEngine.BoundsInt()
    self._unmanagedVar = nil
end

-----@private
--function TypeDefense:OnDestroy()
--    
--end

-----@private
--function TypeDefense:OnDisable()
--    
--end

-----@private
--function TypeDefense:OnEnable()
--    
--end

-----@private
--function TypeDefense:Update()
--    
--end

--endregion

return TypeDefense