---
--- Generated by EBGAME
--- Created by wbn.
--- DateTime: 2022/07/28 09:06:03
---

---@class GetSet : UnityEngine.MonoBehaviour GetSet
---@field varGetSet number
---@field varOnlyGet number
---@field varOnlySet number
---======================== 面板变量 ========================
---=========================================================
local GetSet = class("GetSet", target)

--region ===================== 扩展的数据类型 =====================
--- 放自定义的结构体、枚举等
GetSet._getValue = 1
GetSet._setValue = ""
--endregion

--region ======================== 基类成员 ========================
--定义GetSet 上面field加入代码提示
function GetSet.Get:varGetSet()
    return self._getValue
end

function GetSet.Set:varGetSet(value)
    self._getValue = value
end

function GetSet.Get:varOnlyGet()
    return "OnlyGet"
end

function GetSet.Set:varOnlySet(value)
    self._setValue = value
end
--endregion

--region ======================== 基类方法 ========================

--endregion

--region ======================== Mono接口 ========================

-----@private
--function GetSet:Awake()
--
--end

---@private
function GetSet:Start()
    --调用Get
    Debug.Log(self.varGetSet)
    --调用Set
    self.varGetSet = 123
    Debug.Log(self.varGetSet)

    --调用Get
    Debug.Log(self.varOnlyGet)
    --没有Set 异常报错
    try{
        function()
            self.varOnlyGet = "noSet"
        end,
        catch = function(e)
            Debug.Log(e)
        end
    }

    --没有Get 异常报错
    self.varOnlySet = "inSet"
    try{
        function()
            Debug.Log(self.varOnlySet)
        end,
        catch = function(e)
            Debug.Log(e)
        end
    }
end

-----@private
--function GetSet:OnDestroy()
--    
--end

-----@private
--function GetSet:OnDisable()
--    
--end

-----@private
--function GetSet:OnEnable()
--    
--end

-----@private
--function GetSet:Update()
--    
--end

--endregion

return GetSet